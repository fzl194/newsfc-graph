# CR-20260807-002-业务专题纳入Feature簇构建

> 状态：🟡 待审（临时手工回填已落地；脚本补丁待集成实现）

## 场景

冷启动 Feature 层 `build_features.py` 只扫"特性指南"（`output/.../特性部署/特性指南/UDG特性指南`），**"业务专题"目录（`特性部署/业务专题/`）不在输入**，导致一批特性的**激活/原理/约束/参考信息**文档系统性漏收——簇里只有 `概述.md`。

实测命中（4 簇，分属两个业务专题）：
- **头增强族**（`UDG头增强功能专题/`）：GWFD-110261 HTTP头增强、GWFD-110262 RTSP头增强、GWFD-110263 HTTPS头增强——激活/原理/约束/参考信息 全在业务专题下，簇仅 概述.md。
- **防欺诈族**（`UDG防欺诈功能专题/`）：GWFD-110401 HTTP头防欺诈——同理（无单独参考信息文档）。

诊断要点（支撑新 `语料演进SOP.md`）：症状曾表现为 Task 层（"FT 引用 activation §表N 在簇里找不到"），**根因在 Feature 层簇不全**——跨层根因定位。Task 层 FT 反而建对了（从激活文档正确建出）。

## 规范现状

- `feature/scripts/build_features.py`：`--feature-dir` 单输入，`build_all.py` 固定指向特性指南；`rglob("*.md")` 按路径最深 feature_code 归组（`build_features.py:151-163`）。业务专题平级目录从未被扫。
- `feature/scripts/_common.py:129-140` `_DOC_TYPE_KEYWORDS`：覆盖 激活/去激活/调测/原理/配置/参考信息/影响/部署/术语，**未覆盖"差异"**（`头增强功能之间的差异.md` 会落"其它"）。
- 业务专题根有 `特性映射.md`（`| 功能名称 | 特性ID | 特性名称 | License项 |`），但**只覆盖"原理描述"文档**，激活/调测/差异无显式 code；构建脚本完全不读此文件。
- `feature/check.md`：有"概述存在"审查项，**无"激活文档齐全性"审查项**。
- 已知关联：`task/change-requests/CR-20260807-001` line 35 曾标记 GWFD-110261/110401 簇缺激活（Task 层审计发现，守"不越界修"纪律未动 Feature 层）。

## 当时怎么处理的（2026-08-11，手工回填 stopgap）

按用户授权"手工回填 4 簇 + 核对 FT"，在 Feature 层做了**临时手工回填**（待自动化重建保可复现）：

**B1 · 4 簇子文档回填**（共 15 个 Feature 子文档）：
| 簇 | 新增子文档（slug / doc_type） | 图片 |
|---|---|---|
| GWFD-110261 | 激活HTTP头增强/激活、HTTP头增强原理/原理、约束限制和系统影响/影响、参考信息/参考信息 | 5 张 |
| GWFD-110262 | 激活RTSP头增强/激活、RTSP头增强原理/原理、约束限制和系统影响/影响、参考信息/参考信息 | 6 张 |
| GWFD-110263 | 激活HTTPS头增强/激活、HTTPS头增强原理/原理、约束限制和系统影响/影响、参考信息/参考信息 | 7 张 |
| GWFD-110401 | 激活HTTP头防欺诈/激活、HTTP头防欺诈原理/原理、约束限制和系统影响/影响（**无参考信息源**，未臆造） | 2 张 |

每个子文档：7 字段 frontmatter（`id=UDG@Feature@{code}-{slug}` + doc_type）+ stopgap 注（标 2026-08-11 手工回填 + 引本 CR）+ 改写正文（命令引用 `[**CMD**](url)`→`[[UDG@MMLCommand@CMD]]`、图片→`assets/`）+ `## 边 - 属于特性`。各 概述.md `## 边` 补 `包含子文档` 行。

**B2 · 4 个 FT 核对**（对照新可用激活文档）：
- GWFD-110261/110262/110401：命令闭包+参数闭包 CLEAN。
- GWFD-110263：1 处 MEDIUM（决策点联动行 WELLKNOWNPORT 漏 PRIORITY=10）**已修**。
- 4 个 FT 引子均补链 `[[UDG@Feature@{code}-激活{变体}]]`。
- 发现 activation 文档自身一处不一致（`激活HTTPS头增强.md` §表3 SET REFRESHSRV REFRESHTYPE=USERPROFILE vs 脚本 ALL）——源文档 bug，待自动化重建时核。

**B3 · NSH 头增强标注**（不盲建）：
业务专题 `UDG头增强功能专题/NSH头增强功能/` 有 激活/调测/原理 文档，但 `特性映射.md` **未列 NSH**、无对应 FT、无特性码。**疑似新特性（待产品文档/用户确认特性码，如 GWFD-110264？）**——本轮不盲建，仅记录。

## 建议的规范补丁（脚本侧，待集成实现）

1. **`build_features.py` 加业务专题输入**：新增 `--topic-dir`（或把 feature-dir 抬到 `特性部署/` 同时覆盖特性指南+业务专题）；对业务专题 md，按"特性映射.md"表 + 同专题子目录名启发式归 feature_code。
2. **解析 `特性映射.md`**：建立 topic-doc → feature_code + License 映射，作为业务专题归组依据；建议要求每个业务专题根维护该表。
3. **`_common._DOC_TYPE_KEYWORDS` 补 `("差异","差异")`**：让 `头增强功能之间的差异.md` 正确分类（当前落"其它"）。
4. **`feature/check.md` 增审查项**："激活类特性（有配置）须有激活子文档"——防再次漏收。
5. **白名单防误收**：业务专题含大量非特性内容（如 `5G Core 计费解决方案/` 数千 md），须只收"明确归属特性的专题文档"（经特性映射或子目录名匹配），不误收解决方案/术语/部署总述。

## 影响范围

- **对象类型**：Feature 子文档（Feature 层簇内容）。
- **数量**：本轮 4 簇 15 子文档；全量看，头增强族+防欺诈族+可能业务感知族等所有"有业务专题的特性"都受影响（系统性缺口，规模待全量盘）。
- **已建资产影响**：**Feature 层需重建**（MAJOR 级，待集成执行脚本补丁后）。本轮手工回填的 4 簇 15 子文档在自动化重建前以本 CR 清单为准；重建后由脚本统一产出（slugs/doc_type/图片/ref 规范化），手工资产会被覆盖——**届时核对手工版的 stopgap 注与命令引用转换是否被正确继承**。
- **可复现性声明**（演进机制.md 纪律）：本轮手工回填是**有记录的临时偏离**（本 CR + 各子文档 stopgap 注 + CHANGELOG 0.18.0），不破坏"今天与下次构建一致"——因为下次构建（脚本扩展后）会以规范方式重新产出这些子文档，手工版作为过渡。
- **规范文件**：`feature/scripts/build_features.py`、`feature/scripts/_common.py`、`feature/check.md`；本轮另产出顶层 `语料演进SOP.md`（把本案例沉淀为"模式 A·结构化语料"通用方法）。

## 关联
- 顶层新 SOP：`三层图谱构建规范/语料演进SOP.md`（本 CR 案例为其"模式 A"worked example）。
- Task 层前序：`task/change-requests/CR-20260807-001`（B4 信息源边界审查项；本 CR 是其在 Feature 层的根因修复）。

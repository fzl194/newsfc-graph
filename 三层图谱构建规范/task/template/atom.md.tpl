---
id: "{{nf}}@AtomTask@{{command}}"
type: "AtomTask"
name: "{{command}}"
name_zh: "{{配置动作名}}"
nf: "{{nf}}"
ref: "{{nf}}@MMLCommand@{{command}}"
status: "draft"
---

# {{配置动作名}}（{{command}}）

> 命令静态知识（功能/参数真相表/规格/notes）见 [[{{nf}}@MMLCommand@{{command}}]]。
> 本页只讲配置生成时这条命令**怎么配**——按配置维度组织成"字典"，不逐特性罗列取值。

## 配置方法

{{1-2 段叙述：命令做什么 + 核心配置维度有哪些}}

### 配置维度 1：{{维度名}}（参数 {{PARAM}}）

| 取值 | 作用 | 配套必选参数 | 典型场景 |
|---|---|---|---|
| {{取值}} | {{作用}} | {{配套参数}} | {{场景}} |

> 多维度时每维度一个 `### 配置维度 N` 子节；列按维度需要增减（最少 `取值/作用` 两列，同一 atom 内表头风格尽量一致）。

## 决策点

{{有 DP：| 选项 | 影响（联动参数/场景） |}}
{{无 DP：本命令用法单一，无分支。}}

## 约束

- **{{规则名}}**（{{critical|warning|info}}）：{{约束}} — {{后果}}

## 边
- 对应命令: [[{{nf}}@MMLCommand@{{command}}]]

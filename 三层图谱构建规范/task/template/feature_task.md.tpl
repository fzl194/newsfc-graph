---
id: "{{nf}}@FeatureTask@{{feature_code}}"
type: "FeatureTask"
name: "{{feature_code}}"
name_zh: "{{特性名}}"
nf: "{{nf}}"
ref: "{{nf}}@Feature@{{feature_code}}"
status: "draft"
---

# {{特性名}}（{{feature_code}}）

> 特性静态知识见 [[{{nf}}@Feature@{{feature_code}}]]。本页讲配置生成实例化时怎么配。

## 配置概览

{{对象链 + 场景骨架（2-3 段，含关键差异维度）}}

## 配置流程

> 步骤 + 单命令混合编排。步骤（≥2 命令）→ compound；单命令 → 直接 atom。

1. **{{步骤A名}}**（{{一句话}}）：`{{CMD1}}` + `{{CMD2}}` → [[{{nf}}@CompoundTask@{{英文名}}]]
   - 关键参数：`{{参数1}}`=`<取值>`
2. **{{命令B名}}**（{{一句话}}）：`{{CMDB}}` → [[{{nf}}@AtomTask@{{CMDB}}]]
   - 关键参数：`{{参数}}`=`<取值>`
3. **{{步骤C名}}**（{{一句话}}）：`{{CMD3}}` + `{{CMD4}}` → [[{{nf}}@CompoundTask@{{英文名}}]]
   - 关键参数：`{{参数}}`=`<取值>`

## 激活方法与参数差异

> 每种激活方法/条件至少一行；跨多个相位时分行。必须能还原实际命令时间线，包含执行/省略的 Task、关联 AtomTask、`INHERIT` 和重复刷新。

| 激活方法/条件 | 配置相位 | 执行的 Task（CompoundTask / AtomTask） | 省略的 Task | 关联 AtomTask | 相对基线的参数差异（参数=值） | 目标对象与生效说明 |
|---|---|---|---|---|---|---|
| {{方法/条件}} | {{相位}} | [[{{nf}}@CompoundTask@{{英文名}}]] / [[{{nf}}@AtomTask@{{CMD}}]] | {{无则写无}} | [[{{nf}}@AtomTask@{{CMD}}]] | `{{参数}}`=`<值>`；{{无差异则写与基线相同}} | {{对象、条件、生效/刷新位置}} |

## 参数核对

| 场景/命令 | 关联 AtomTask | 实际参数=值 | 核对结论 |
|---|---|---|---|
| {{场景/命令}} | [[{{nf}}@AtomTask@{{CMD}}]] | `{{参数}}`=`<值>` | {{通过 / 冲突，待 Atom 更正 / 待数据规划补齐}} |

## 决策点

> 场景影响表；多 DP 按差异独立轴分表（≥2 轴）。无分支显式说明。DP 不编号。

### DP1：{{决策点名}}
| 选项/场景 | 走法 | 关键联动 | 影响 |
|---|---|---|---|
| {{选项}} | {{compound/atom}} | {{参数}} | {{影响}} |

## 约束

- **{{规则名}}**（{{severity}}）：{{约束}} — {{后果}}

## 边
- 对应特性: [[{{nf}}@Feature@{{feature_code}}]]
- 编排: [[{{nf}}@CompoundTask@{{英文名}}]], [[{{nf}}@AtomTask@{{CMD}}]]

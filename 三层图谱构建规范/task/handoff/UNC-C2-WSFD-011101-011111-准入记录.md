# UNC-C2（WSFD-011101、011102、011107、011108、011111）输入与准入记录

> 记录日期：2026-08-13  
> 用途：FeatureTask 落盘前的只读输入、Atom 准入和 CompoundTask 候选判定。未使用原始产品文档例外。

## WSFD-011101：Gb 接口

- **完整 Feature 文档簇**：`概述.md`、`实现原理.md`、`WSFD-011101 Gb接口参考信息.md`。
- **可确认责任/场景**：SGSN 与 GSM BSS 的 Gb over IP；原理区分静态 NS-VC 与动态自动配置。动态方式明确预置端点使用 `ADD GBEPPOOL`、`SET GBLOCPORTRGE`，但未给实例、对象名或完整时间线；参考信息仅列相关命令。
- **配置命令与 Atom 路径**：已存在：`ADD NSE`、`MOD NSE`、`SET BSSGP`、`SET GB`、`ADD GBNSECFGPARA`、`ADD GBNSEGRP`、`ADD BSSIDFORNSEI`（各路径均为 `三层图谱资产/AtomTask/UNC/20.15.2/UNC@AtomTask@{命令}.md`）；缺失：`ADD GBEPPOOL`、`SET GBLOCPORTRGE`、`RMV NSE`、`SET FLOWCTRLBUF`、`ADD GBIPLOCENDPT`、`ADD GBIPRMTENDPT`。文档未给任何 `参数=值`，无法做参数准入。
- **候选 CT**：无。不存在可恢复的 ≥3 命令稳定时间线；不可将 SIZE/CONFIG、BVC 等协议消息当作 MML 步骤。
- **结论**：information-limited；写 `draft` FT，不编排 Atom/CT。

## WSFD-011102：Iu 接口

- **完整 Feature 文档簇**：`概述.md`、`实现原理.md`、`WSFD-011102 Iu接口参考信息.md`。
- **可确认责任/场景**：SGSN 与 RNC 的 Iu 承载和控制面接口。参考信息列出 SCCP、RNC、M3UA 与 Iu paging 的对象命令，但没有激活方法、数据规划、脚本、参数值或对象创建顺序；RAB、Iu 信令连接和加密均为协议消息流程。
- **配置命令与 Atom 路径**：已有 Atom：`ADD SCCPOPC`、`ADD SCCPDPC`、`ADD SCCPSSN`、`ADD RNC`、`MOD RNC`、`ADD M3LE`、`ADD M3DE`、`ADD M3RT`、`ADD M3LKS`、`ADD IUPAGING`；缺失 Atom：所有参考信息列出的 `RMV SCCPOPC/MOD SCCPOPC/RMV SCCPDPC/MOD SCCPDPC/RMV SCCPSSN/MOD SCCPSSN/RMV RNC/RMV M3LE/MOD M3LE/RMV M3DE/MOD M3DE/RMV M3RT/MOD M3RT/RMV M3LKS/MOD M3LKS/ADD M3LNK/RMV M3LNK/MOD M3LNK`。无 `参数=值` 实例，已有 Atom 亦无法完成本 Feature 准入。
- **候选 CT**：无。对象列表未提供相对顺序；不以 RAB/加密/SCTP 协议过程建立 CompoundTask。
- **结论**：information-limited；写 `draft` FT，不编排 Atom/CT。

## WSFD-011107：Gn 接口

- **完整 Feature 文档簇**：`概述.md`、`特性概述.md`、`实现原理.md`、`WSFD-011107 Gn接口（适用于MME）参考信息.md`、`WSFD-011107 Gn接口（SGSN_GGSN）参考信息.md`。
- **可确认责任/场景**：同 PLMN 内 GSN 间 Gn，覆盖 SGSN/GGSN 与 MME 两个适用口径；两份参考信息均只列 `ADD GGSNCHARACT`，无参数或顺序。路径探测、隧道/位置/移动管理是协议或转交给其他 Feature 的运行时流程。
- **配置命令与 Atom 路径**：`ADD GGSNCHARACT` Atom 已存在：`三层图谱资产/AtomTask/UNC/20.15.2/UNC@AtomTask@ADD GGSNCHARACT.md`；本 Feature 未给参数实例，不能核验其配置方法字典。
- **候选 CT**：无。仅一个无实例的命令候选，且没有可恢复激活链。
- **结论**：information-limited；写 `draft` FT，不编排 Atom/CT。

## WSFD-011108：Gp 接口

- **完整 Feature 文档簇**：`概述.md`、`实现原理.md`、`WSFD-011108 Gp接口参考信息.md`。
- **可确认责任/场景**：跨 PLMN GSN 的 Gp 接口；参考信息只列 `ADD GGSNCHARACT`。路径、隧道、位置和移动管理属于协议运行期或被明确导向会话/移动性/路径探测 Feature。
- **配置命令与 Atom 路径**：`ADD GGSNCHARACT` Atom 已存在：`三层图谱资产/AtomTask/UNC/20.15.2/UNC@AtomTask@ADD GGSNCHARACT.md`；无 Feature 参数=值、对象实例或命令顺序，未通过本 Feature 参数准入。
- **候选 CT**：无。单个无实例参考命令低于抽取门槛，也不能由协议流程推断步骤。
- **结论**：information-limited；写 `draft` FT，不编排 Atom/CT。

## WSFD-011111：SS7 接口

- **完整 Feature 文档簇**：`概述.md`、`WSFD-011111 SS7接口参考信息.md`。
- **可确认责任/场景**：SGSN 通过 SS7 over IP 对接 HLR/MSC/EIR/SMS GMSC/RNC；参考信息列出 SIGTRAN、M3UA、SCCP 对象命令。概述仅点名 `ADD SCCPNGT` 的 `ITUTT` 和 `SET SIGATTR` 的协议类型，未提供任何取值或开通顺序。
- **配置命令与 Atom 路径**：已有 Atom：`SET SIGATTR`、`ADD M3DE`、`ADD M3LKS`、`ADD M3LE`、`ADD M3RT`、`ADD SCTPPARA`、`ADD SCCPDPC`、`ADD SCCPGT`、`ADD SCCPOPC`、`ADD SCCPSSN`、`SET SCCPTMR`；缺失 Atom：`ADD M3LNK`、`ADD SCCPDPCSHR`、`ADD SCCPNGT`。全部命令均无本 Feature 的 `参数=值` 和对象实例。
- **候选 CT**：无。命令目录没有时间线；不可把 SS7 协议/寻址/GT 翻译原理误写为步骤。
- **结论**：information-limited；写 `draft` FT，不编排 Atom/CT。

## 原始文档例外

未使用。现有完整 Feature 文档簇足以确认独立接口配置责任及“缺命令实例、顺序和参数”的事实；回查原始文档不能绕过缺失 Atom 或把静态原理替换为激活输入。

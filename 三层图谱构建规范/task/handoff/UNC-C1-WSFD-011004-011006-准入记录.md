# UNC-C1 子范围：WSFD-011004、WSFD-011005、WSFD-011006 输入与准入记录

> 记录日期：2026-08-13  
> 用途：本文件是 C1 批次交接的只读输入与准入记录；不替代对应 FeatureTask 正文，也不构成额外资产层。

## 总体输入边界

- 已完整读取三个 Feature 目录下的全部 `*.md`，共 6 篇：每个特性均为 `概述.md` 与 `参考信息.md`。
- 已读取 `CompoundTask/UNC/20.15.2/_index.md` 全文（55 个 CompoundTask），并按命令集和配置目标评估候选。
- 对实际配置类命令已读取 AtomTask；同时为参数准入澄清读取相关命令层 md。
- 未使用原始产品文档例外回查：Feature 文档簇足以判定是否存在独立配置责任、可恢复命令链及参数/顺序缺口。

## Feature 级记录

### WSFD-011004

- **已读 Feature md**：`概述.md`；`WSFD-011004 Gn接口同时支持GTPv0和GTPv1参考信息.md`。
- **场景与配置责任**：SGSN 默认同时支持 GTPv0/GTPv1，并在收发消息时自动按对端版本协商/回退；参考信息明确“本特性无相关命令”。
- **配置类命令与 AtomTask**：无。测量指标不是配置输入。
- **CT 候选**：无命令集，Jaccard 不适用；不建 CT。
- **原始文档回查**：否；不存在需由原文解决的命令、参数或顺序问题。
- **准入结论**：`foundation`。完整簇确认这是默认协议兼容能力，未给出独立 UNC 本地配置责任或流程。

### WSFD-011005

- **已读 Feature md**：`概述.md`；`WSFD-011005 支持R98_R99_R4_R5_R6_R7的话单格式参考信息.md`。
- **场景与配置责任**：SGSN 可按 2G/3G 用户分别选择 CDR 协议格式并发往 CG；概述将该责任明确归于 `SET CHGGA`。`LST CHGGA`、`DSP CHGGA` 仅为查询/调测，不进入 Task 流程。
- **配置类命令与 AtomTask**：`SET CHGGA` → `三层图谱资产/AtomTask/UNC/20.15.2/UNC@AtomTask@SET CHGGA.md`，文件存在；Atom 允许 `GCR` 的 R98/R99/R4/R5/R6/R7 与 `UCR` 的 R99/R4/R5/R6/R7 取值。Feature 未给实际 `param=value`、CG 对接版本、对象实例或执行相位，故无逐值“通过”结论。
- **CT 候选**：仅 1 个 distinct 配置 atom；按 SOP 抽取 floor 直引 Atom，Jaccard 不适用，不建 CT。
- **原始文档回查**：否；缺口是 Feature 自身没有激活脚本/数据规划，原始文档不能替代该输入。
- **准入结论**：`information-limited` / draft 直引 Atom。待补 CG 协议版本、2G/3G 实际格式选择、`CHGVER*`（如适用）、执行/重启相位及数据规划。

### WSFD-011006

- **已读 Feature md**：`概述.md`；`WSFD-011006 支持R97_98 QoS和R99 QoS之间的映射参考信息.md`。
- **场景与配置责任**：SGSN 在 R97/98 与 R99 QoS 互操作时执行协议映射；概述明确 `SET COMPATIBILITY` 的 `RLBCLSMAP` 控制 Reliable Class 映射的两个选择。`LST COMPATIBILITY` 为查询，不进入 Task 流程。
- **配置类命令与 AtomTask**：`SET COMPATIBILITY` → `三层图谱资产/AtomTask/UNC/20.15.2/UNC@AtomTask@SET COMPATIBILITY.md`，文件存在；Feature 明确给出 `RLBCLSMAP=PROHIBIT/ALLOW`，Atom 允许二者并说明其 Reliable Class 后果。Feature 未涉及的其他可选参数不凭 Atom 默认值补写。
- **CT 候选**：仅 1 个 distinct 配置 atom；按 SOP 抽取 floor 直引 Atom，Jaccard 不适用，不建 CT。
- **原始文档回查**：否；Feature 已足以确认分支和输入缺口，原始文档不用于补造参数/时序。
- **准入结论**：`ready` / 单 Atom 直引双分支。`RLBCLSMAP=PROHIBIT/ALLOW` 均可执行；不建 CT。Feature 未涉及的其余命令参数不作前置或臆造。

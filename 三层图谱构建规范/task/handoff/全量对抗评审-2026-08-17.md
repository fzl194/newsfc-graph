# UDG + UNC Task 层全量对抗评审报告（FeatureTask + CompoundTask）

> 评审日期：2026-08-17 ｜ 评审范围：**不审 AtomTask**（用户指令）｜ 依据：task/check.md + SKILL.md §B（v0.19.0）
> 方法：结构全量（419 FT + 105 CT 脚本扫描 + audit D0-D5）+ 语义抽样深审（高风险加权 87 FT + 19 CT，6 个独立只读 Agent 逐场景对照 Feature 簇）
> 性质：独立对抗评审（评审方未参与构建、未修改资产）

---

## 一、总览

| 层级 | CRITICAL | HIGH | MEDIUM | LOW |
|---|---:|---:|---:|---:|
| 语义抽样（87 FT + 19 CT 深审） | 9 | 35 | ~60 | ~24 |
| 结构全量（419 FT + 105 CT） | 0 | 0 | 224（S2 版本代差 200 + S4 防平铺 24） | 少量 |
| 存量已知（迁移 audit 发现） | — | 2 | — | — |

抽样对象 CLEAN 率：FT ~60/87（69%），CT ~6/19（32%——重型共享 CT 场景差异普遍不全）。

## 二、CRITICAL（9，全部为 B1/B2 信息丢失，须返工）

| # | 对象 | 问题 | 修复建议 |
|---|---|---|---|
| 1 | UNC@FeatureTask@WSFD-011201 | SGSN 侧离线计费**整组命令静默丢失**（ADD CHGCG/CHGAPN/CHGDCHAR/9×CHGIMSICFG/2×CHGPLMNCFG、SET CHGCDR + 话单可选功能/字段/费率切换/可靠性多份配置文档） | 近似需重建：按簇补激活方法表+编排 |
| 2 | UNC@FeatureTask@WSFD-011206 | SET CTXSTARTRATING（预申请配额 RG 子场景）FT 与全部 9 个 CT 无还原无省略 | 补编排或显式省略 |
| 3 | UNC@FeatureTask@WSFD-109101 | 配置异常场景数据.md 的 SET PCCFAILACTION / ADD RESULTCODECTRL / SET FHBYPASS 整组静默丢失 | 补异常场景分支 |
| 4 | UDG@FeatureTask@GWFD-020406 | 4 篇变体文档步骤 7b 的 SET APNIPALLOCRULE 全缺（atom 已存在） | 单命令补齐 |
| 5 | UDG@FeatureTask@GWFD-020412 | 两方式脚本的 ADD APN: APN="apn-l2tp" 前置全缺（atom 已存在） | 单命令补齐 |
| 6 | UDG@FeatureTask@IPFD-015004 | 普通 IPv4 activation 的 MOD INTERFACE 静默丢失（ipsec-tunnel-setup command_set 22 命令无它） | FT 或 CT 二选一回填 |
| 7 | UDG@FeatureTask@GWFD-112000 | 两任务脚本均含 ADD RULE（rule_ims_signaling PRIORITY=241 / rule_ims_voice 242），编排链无承载 | 改引 rule-userprofile-bind 或 atom 直引 |
| 8 | UDG@FeatureTask@IPFD-012002 | 簇内《去激活支持ACL/ACL6》7 条 RMV 命令静默丢失 | 补去激活链（atom 已有 4/7） |
| 9 | UDG@FeatureTask@GWFD-110263 | ADD WELLKNOWNPORT 的 IDENPROTNAME/PORTOP 两参数静默丢失（v0.18.0 回填对象重点核对未通过） | 补参数 + 参数核对段 |

## 三、HIGH（35，按模式归类）

**H-1 激活方法表缺失/缩略化（8 处：110311/015004/107021/020401/020306/011201/104001/020423-12atom）**
共同模式：多场景命令时间线压进 DP 文字，"执行的 Task 写同上/同基线+XX段"，违反 §B.5 时序不可泛化。

**H-2 共享 CT 场景差异回填缺失（R1.2，7 处重型 CT + 3 处 FT 侧）**
- charging-core-trio（28 引用 FT 仅部分有差异行）、charging-tail（7 FT 无逐行）、filter-chain（35 FT 中 16 无逐行）、rule-userprofile-bind（27 FT 无逐行）、license-access-prep（3 FT 无行）、pcc-predefined-rule-chain（2H：ADD BWMSERVICE 命令越界进步骤表但不在 command_set；场景差异错挂非引用方 GWFD-020351）
- GWFD-020359 → filter-chain 无回填行；GWFD-010104 → addr-pool-hierarchy EXTERNAL 变种无回填；GWFD-010107 → addr-pool-hierarchy 无行且约束矛盾
- 系统性问题：大量差异行以旧编号（2-000xx）标注无 GWFD 映射，无法闭环验证

**H-3 B3 参数臆造/参数名漂移（4 处）**
- GWFD-020411 三处参数名与 atom/命令层字典不符（TARGETTYPE/TARGETVALUE 应为 VRFRTTYPE/VRFRTVALUE；LABELMODE 应为 VRFLABELMODE；REFLECTORCLIENT 不存在）——**该 FT 取值将作下游字典源，需优先修**
- GWFD-020406 变体5 IPV6SWITCH=ENABLE vs 脚本 SWITCH=ENABLE
- WSFD-104001 2-3G License 项 LKV2IPV601/LKV2IPV6SM01 实有值却标"待核"（漏配前置风险）

**H-4 内部矛盾/stale（5 处）**
- IPFD-012002（UDG）："atom 待补建"声明过期（atom 实已存在，三处口径矛盾），连带 acl-rule-chain CT command_set 缺 2 条可准入命令
- session-pcc-policy：正文两次声明"无独立命令"但 command_set 含 3 命令（foundation 叙事与元数据矛盾）
- charging-tail 场景差异挂错对象（110311 非其引用方）；license-access-prep 差异行挂 GWFD-020301（非引用方）+ 标签错
- UNC _index.md 漏登 4 个 DRA CT（dra-diameter-transport-route / gx-dra-pcc-realm-enable / gy-dra-ocs-direct-peer / s6b-dra-aaa-binding）——compound_count 55 vs 实际 59，**重跑 gen_compound_index.py 即修**
- 存量已知 2 处（pcc-predefined-rule-chain D3 反链 stale GWFD-020354；IPFD-014000 UDG Feature 层缺建致 020423 断链——Feature 层问题）

**H-5 其他（边缺失 3 处：WSFD-211009/IPFD-015004/WSFD-107021 正文编排 CT/atom 但 ## 边 无编排边；GWFD-020412 时序依赖倒置 L2TPGROUP；WSFD-011201/011206/109101 的 HIGH 附属项）**

## 四、MEDIUM（~60 + 结构层 224）

- **版本代差系统性项（结构层 200）**：v0.17.0 前 FT 缺 `## 参数核对` 段 70 例、缺激活方法表 46 例、foundation 缺决策点/约束 78 例（foundation 是否必须决策点/约束段属规范灰色地带，建议 CR 澄清）
- R1.2 场景差异按"场景族"组织无 FT↔行映射（bwm-*/sa-*/header-enrich 等 6 CT）
- B2 参数值散落无落点（各批 ~20 例）
- 防平铺评估提示 24 例（其中 020423 12-atom 已自述理由，合规）
- 旧编号残留（charging-core-trio 等，LOW~MEDIUM）
- 空 evidence 括号残留若干（无证据规范清理不彻底）

## 五、亮点（抽样确认的正确实践）

- 计费族 CT 对 020306/020351/020357/020358/110203 回填完整；020358 REFRESHTYPE=USERPROFILE 原相位保留并显式说明；110203 SVRIPMODE 冲突标"待 Atom 更正"未伪装
- 020423（除已知断链外）CLEAN：命令闭包/时序/BFD 二选一/两处冲突处理全对
- UNC B6 域（010801/010802）与 A1-A2 域（IPFD-012001/012002/016000）大量 CLEAN——近期批次质量显著好于早期
- 全部 105 CT 反链数量与 _index 登记一致（除已知 1 例 + UNC 4 漏登）
- 调测剥离全量通过（16 个初扫命中全部为合法显式说明）

## 六、共性根因与建议

1. **B1 静默丢失集中在两类**：可选分支/异常场景命令组（011201/109101/110263）、前置依赖命令（020406/020412/015004/112000）→ **D6 命令闭包自动化（CR-20260807-001 已有规格）应尽快落地**，人工评审已证明无法全拦。
2. **旧编号（2-000xx）无映射表**致 R1.2 无法闭环 → 建议一次性建 旧编号→feature_code 对照表 或直接把 CT 差异行改写为 GWFD 码。
3. **版本代差**（缺参数核对/激活表）集中在 v0.17.0 前批次 → 建议按域排期升级，与 HIGH 返工同批处理。
4. **参数名漂移**（020411 类）建议 audit 加 D8：FT/CT 正文参数名 ⊆ 对应 atom 命令层参数表（可机器判定）。

## 七、返工优先级

| 优先级 | 对象 | 理由 |
|---|---|---|
| P0 | WSFD-011201 | 整组命令丢失，近似重建 |
| P0 | 020406 / 020412 / IPFD-015004 / 112000 / IPFD-012002(UDG) / 110263 | 单点 CRITICAL，快速补齐 |
| P0 | WSFD-109101 / 011206 | 场景组丢失 |
| P1 | GWFD-020411 | 参数名漂移将污染下游 |
| P1 | UNC _index 重生（4 DRA CT） | 一条命令即修 |
| P1 | 8 处激活方法表补全（H-1） | 时序不可泛化 |
| P2 | R1.2 共享 CT 差异回填 + 旧编号映射 | 集成轨统一处理 |
| P2 | session-pcc-policy 叙事矛盾、stale 差异行清理 | 顺手修 |
| P3 | 版本代差参数核对段补齐 | 按域排期 |

---

*本报告由 6 个独立只读评审 Agent 并行产出合并；详细逐对象证据（文件:行号/原文引述）见各批次交付原文，本报告保留汇总与可定位索引。*

---

## 八、修复记录（2026-08-17 同日，全部先对照 Feature 簇验证）

**验证原则**：每个评审发现先回特性层资产 md（必要时回查 output/ 原始特性指南）确认真伪；误报不修并记录；信息不足如实标"待数据规划补齐/待回查"。

### CRITICAL ×9 全闭环
| # | 对象 | 修复 |
|---|---|---|
| 1 | WSFD-011201 | SGSN 分支 11 步链全收编（SET CHGPLMNCHAR/ADD CHGPLMNCFG/ADD CHGIMSICFG(6参数)/SET CHGCDR/SET CHGCHAR/ADD CHGAPN/ADD CHGDCHAR/SET CHGGA/费率切换 3 命令/ADD CHGCG 转介）+ 激活表 + 信息源边界；候选 CT `sgsn-offline-charging-chain`（不新建） |
| 2 | WSFD-011206 | 补 8 命令（SET CTXSTARTRATING/ADD N40MSGTEMP/SET GLBCHARGECHAR/SET GLBURRGROUP/ADD PNFSUPI/ADD PNFGPSI/ADD QUOTAEXHAUSTACT/MOD PDUSCACT）+ 新建激活表 8 行 + 参数核对 8 行 |
| 3 | WSFD-109101 | 异常场景 3 命令（SET PCCFAILACTION/ADD RESULTCODECTRL/SET FHBYPASS）独立方法 D + SET PCCPCRFMSGATTR/ADD SPECIFICAPNVAL/ADD QOSPROP 补编排；激活表 A1-D 全景 |
| 4 | GWFD-020406 | SET APNIPALLOCRULE 补步骤 7b + 4 变体激活表行 + 边（标"待数据规划补齐"） |
| 5 | GWFD-020412 | ADD APN 前置补步骤 4b + 相序修正（L2TPGROUP 先于 ADD APN，按脚本）+ 激活表 2 主行 + 边 |
| 6 | IPFD-015004 | 子 Agent 重建：MOD INTERFACE 步骤 3 + 激活表 15 行（16 场景）+ 参数核对 + 编排边（17 atom 全验） |
| 7 | GWFD-112000 | ADD RULE 改引 rule-userprofile-bind（双向反链同步：bind +112000 场景差异行，attach 移除 112000） |
| 8 | IPFD-012002 | 去激活链补（RMV ACLRULEADV4/GROUP + ADV6/GROUP6 编排；IF/ETH/BAS4 三 atom 标待补建阻塞）；stale"待补建"陈述更正（atom 实已建） |
| 9 | GWFD-110263 | WELLKNOWNPORT 补全 5 参数（IDENPROTNAME=https_443/PORTOP=EQUAL）+ SET SOFTPARA BYTE896 编排 + 边 |

### HIGH 修 12 项
- **GWFD-020411**（P1 参数污染）：TARGETTYPE/TARGETVALUE→VRFRTTYPE/VRFRTVALUE、LABELMODE→VRFLABELMODE；`ADD BGPPEER REFLECTORCLIENT=TRUE` 核正（命令层无此参，RR 参数实在 ADD/MOD BGPVRFAF 的 REFLECTORCLUSTERID/IPV，簇内仅概念——标待数据规划）；SET BGPGR/BGPNSR/VPNFRR/MOD BGPGLOBAL/SET BGPLABELMODE 均查命令层+原始指南无据（原始指南 L125-129 仅概念叙述）→ 改"待回查"如实标注
- **激活表补全 6**：110311（9 行）/107021（5 行）/020306（场景 2-4 展开+SET SPECTRAFURRGRP 改必配）/020401（PD/双栈行展开自含）/104001（3 行+License LKV2IPV601/LKV2IPV6SM01 回填）/211009（边补 rate-id-chain）
- **其他**：session-pcc-policy 叙事矛盾（command_set 改"依赖指针"语义+修我修复时引入的 D3 误报）；filter-chain 补 020359 行；addr-pool-hierarchy 补 010104 EXTERNAL 行+约束矛盾修正；010104 CHECKIPVALID 降级"可选"；UNC _index 重生（4 DRA CT 入册 55→59）；pcc-predefined-rule-chain D3 stale 删除+错挂 020351 行删+ADD BWMSERVICE 越界标注归属

### 评审发现中的误报（不修，记录）
- S3 调测 16 条：全部为合法显式剥离说明（"查询命令仅提供取值"等）
- S5 激活表列 11 条：全部为 foundation 合法变体场景表
- 020423 12-atom 平铺：FT 已自述论证（分支多直引不抽 CT）

### 未修（评估后留待）
- D4 IPFD-014000：UDG Feature 层缺建"路由功能"特性（UNC 侧存在）——**跨层问题留用户决策**（Feature 层补建 or FT 改标注）
- 版本代差缺参数核对段 70 例（P3 排期）
- 旧编号 2-000xx 无 GWFD 映射（建议一次性建对照表）
- charging-core-trio 等 R1.2 场景差异按族组织无逐 FT 行（集成轨统一）

### 终态验证
- `audit_compound_feature --nf UNC`：**fail=0 全过**
- `audit_compound_feature --nf UDG`：**fail=1**（仅 IPFD-014000，D3 已清零）
- `audit_atoms`：UDG 444/UNC 1325 抽查 100% 合规
- pytest 9 绿；9 CRITICAL 抽验全落位 ✓

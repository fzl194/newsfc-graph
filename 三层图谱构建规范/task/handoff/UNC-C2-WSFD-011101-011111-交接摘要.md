# UNC-C2（WSFD-011101、011102、011107、011108、011111）交接摘要

> 执行日期：2026-08-13  
> 子范围状态：**已完成（5 项信息受限 draft FeatureTask；无新增/修改 CompoundTask）**。

## 输入与产物

- 准入记录：[UNC-C2-WSFD-011101-011111-准入记录.md](D:\mywork\KnowledgeBase\NewSFCGraph\三层图谱构建规范\task\handoff\UNC-C2-WSFD-011101-011111-准入记录.md)。五个 Feature 的完整静态文档簇均已读取；未使用原始产品文档例外。
- 新建 FeatureTask：`WSFD-011101`（Gb）、`WSFD-011102`（Iu）、`WSFD-011107`（Gn）、`WSFD-011108`（Gp）、`WSFD-011111`（SS7）。
- 无 Candidate CT 满足准入：全部缺可恢复的命令时间线、对象实例或 `参数=值`；因此不直引 Atom、不新建/复用 CT，也未修改共享 CT 或 `_index.md`。

## 关键结论

| Feature | 可确认的独立责任 | 未编排原因 / 待补 |
|---|---|---|
| `011101` | Gb over IP 的静态/动态接口配置 | 缺静态与动态链的 MML、对象、参数和顺序；动态必需的 `ADD GBEPPOOL`、`SET GBLOCPORTRGE` Atom 也缺失。 |
| `011102` | RNC—SGSN Iu 对接对象 | 仅命令目录；缺实例和顺序，且部分 SCCP/M3UA 命令缺 Atom。 |
| `011107` | 同 PLMN Gn 接口 | 仅 `ADD GGSNCHARACT` 目录项，无对象、参数、适用口径和相位。 |
| `011108` | 跨 PLMN Gp 接口 | 仅 `ADD GGSNCHARACT` 目录项，无对象、参数和顺序。 |
| `011111` | SS7 over IP 对接 | SS7 命令目录无拓扑、参数和时间线；`ADD M3LNK`、`ADD SCCPDPCSHR`、`ADD SCCPNGT` 缺 Atom。 |

所有 FT 均明确将 SIZE/CONFIG、RAB/Iu 连接/加密、GTP 路径/隧道/位置/移动管理、SS7/GT 协议与寻址原理排除出配置编排。

## 局部核验

- 五份 FT 的 YAML、七个固定正文段和对应 Feature 边均存在；没有 CompoundTask 引用或需反链回填。
- `audit_compound_feature.py --nf UNC --version 20.15.2`：`fail=0`（运行时全局计数：AtomTask 1350、CompoundTask 55、FeatureTask 170）。

## 集成待办

- 集成者可将本摘要和准入记录汇总至 `UNC-C2-交接.md`；本子范围没有 `_index.md` 变更。
- 后续若补齐任一 Feature 的 MML 时间线与参数实例，应重新做 Atom 参数准入，再决定直引/CT；不得从本批接口原理反推命令链。
